# La Habra Pest Control rebuild intake

Repo was empty on clone. Live legacy site at `http://lahabrapestcontrol.com/` returned one PHP homepage and a sitemap with seven URLs: `/`, `/index.php`, `/la-habra-pest-control.php`, `/local-pest-control-service.php`, `/pest-control-90631.php`, `/pest-control-90632.php`, and `/pest-control-90633.php`. Legacy service URLs such as `/ant-control/` and `/rodent-control/` returned 404. Ahrefs organic keyword check for `lahabrapestcontrol.com`, mode `subdomains`, country `US`, returned no ranking keywords. All legacy sitemap URLs are preserved through the homepage, the new service-area page, and Vercel permanent redirects.

Legacy facts found: title `La Habra Pest Control | (949) 339-1111 Bug Flip`, meta description mentioned affordable La Habra pest control and `(949) 339-1111`, visible Bug Flip Inc. text, phone numbers including `(949) 339-1111`, `1 800 284-3547`, `1 714 340-1111`, and `1 562 521-1111`, license text `Lic. PR7771`, and an Orange mailing address. New public site avoids showing a street address and uses call verification language instead of unsupported reviews, 24 hour claims, or guarantees.

Ahrefs SERP checks showed direct ranking examples for `pest control La Habra` and `exterminator La Habra`: The Bugman, Griffin Pest Management, Lady Bugs, Lloyd Pest Control, Pro Pacific, Wheeler's, and Nexgen. Common winning patterns: exact city plus pest-control titles, phone prominent in header, service type sections, pest lists above or near the fold, trust/process copy, FAQs, and location-specific paragraphs. Directory/list pages also appear, but direct operators are the build model.
